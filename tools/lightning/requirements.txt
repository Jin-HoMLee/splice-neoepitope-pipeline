# Lightning AI SDK - programmatic Studio control for free-GPU tFold-TCR runs (Issue #1249).
# One-time setup:
#   uv venv --seed tools/lightning/.venv
#   uv pip install --python tools/lightning/.venv/bin/python -r tools/lightning/requirements.txt
#   tools/lightning/.venv/bin/lightning login   # stores creds in ~/.lightning/credentials.json
# Pinned to the version verified live in #1249 - the runner leans on specific SDK
# behavior (the required org= kwarg, Machine.L40S, studio.status/machine, run() raising
# on non-zero exit). An unpinned bump could shift any of those silently.
lightning-sdk==2026.07.09.post0
