name: splice-neoepitope-optitype
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - optitype =1.3.5
  - samtools >=1.20   # fastq extraction from BAM if needed
  - coincbc           # CBC ILP solver for faster / threaded OptiType runs
