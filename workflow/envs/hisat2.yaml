name: splice-neoepitope-hisat2
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - hisat2 >=2.2.1
  - python >=3.11,<3.13
  - pandas
  - regtools >=1.0.0
  - samtools >=1.20   # 1.20+ supports libdeflate >=1.26, resolving the prior conflict with regtools
